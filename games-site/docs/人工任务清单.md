# 人工任务清单

> 需要人工处理的事项统一记录在此。主 Agent 发现需要人工介入时 MUST 写入本文件。
> 用户定期检查本文件，完成后更新状态。
> 格式：状态 | 任务 | 详情 | 记录日期 | 完成日期

## 待处理

| 状态 | 任务 | 详情 | 记录日期 |
|:--:|------|------|:--:|
| ⏳ | AdSense 审核跟进 | 已提交 2 站（themoundwiki + sephiriawiki），审核 1-14 天。结果发 Gmail，需检查邮件（含垃圾箱） | 2026-08-24 |
| ⏳ | GA4 Property 创建 | doloctownwiki 尚未配置 GA4（googleAnalyticsId 为空）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 | 2026-08-22 |
| ⏳ | GA4 Property 创建（sovereigntowerwiki） | BUILD-002 新站（8/25 建）GA4 留空（googleAnalyticsId: ''）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 + Dashboard 注册（dashboard/lib/sites.ts） | 2026-08-25 |
| ⏳ | GA4 Property 创建（crimsonmoonwiki） | BUILD-003 新站（8/28 建）GA4 留空（googleAnalyticsId: ''），Dashboard 已注册（propertyId PENDING）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 | 2026-08-28 |
| ⏳ | GA4 Property 创建（welcomeelderfieldwiki） | BUILD-004 新站（8/30 建）GA4 留空（googleAnalyticsId: ''），Dashboard 已注册（propertyId PENDING）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 | 2026-08-30 |
| ⏳ | GA4 Property 创建（fallentearwiki） | BUILD-005 新站（8/31 建）GA4 留空（googleAnalyticsId: ''），Dashboard 已注册（propertyId PENDING）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 | 2026-08-31 |
| ⏳ | GA4 Property 创建（bombananawiki） | BUILD-006 新站（9/1 建，9/2 发售）GA4 留空（googleAnalyticsId: ''），Dashboard 已注册（propertyId PENDING）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 | 2026-09-02 |
| ⏳ | GA4 Property 创建（anomalypresidentwiki） | BUILD-007 新站（9/6 建）GA4 留空（googleAnalyticsId: ''），Dashboard 已注册（propertyId PENDING）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 | 2026-09-11 |
| ⏳ | GA4 Property 创建（dressmakerwiki） | BUILD-008 新站（9/12 建，**9/21 发售**）GA4 留空（`googleAnalyticsId: ''`，**非** G-PLACEHOLDER），Dashboard 已注册（propertyId PENDING）。需在 GA 后台创建 property，拿到 measurement ID 后交给主 Agent 填入 `dressmakerwiki/lib/seo-config.ts` + `dashboard/lib/sites.ts` | 2026-09-12 |
| ⏳ | GSC 验证 + 提交 sitemap（dressmakerwiki） | 验证文件 `public/google12f8715471cef7b7.html` 已部署且线上 200；需在 GSC 添加 property 并提交 `https://dressmakerwiki.vercel.app/sitemap.xml` | 2026-09-12 |
| ⏳ | 9/21 发售日内容刷新（dressmakerwiki） | 发售前建站，多项仍为 `[Unconfirmed]`：价格未公布、成就列表未公开、itch 原型是否仍在。发售日需复核并转 confirmed；关键词报告建议 **9/22–9/25 重跑一次采集**（发售前 72% 的种子词零 suggestion） | 2026-09-12 |
| ⏳ | dressmakerwiki 无已验证 itch.io 深链 | 构建时无法从沙箱访问 itch.io（curl/WebFetch 均被拦），且事实表/关键词报告均无已验证 URL → 站内**未硬编猜测深链**，改为链 `https://itch.io/search?q=dressmaker`。若上游存在已验证 URL，请提供后替换 | 2026-09-12 |
| ⏳ | Reddit 采集通道修复（影响每日扩充） | 9/6–9/11 连续多日 `opencli reddit` 报 AUTH_REQUIRED、`/api/me.json` 403、Chrome 无登录态 → 5 个 PV>1000 站的玩家反馈采集**全部缺失 Reddit 这一路**（Steam 评测/讨论区仍可用，未编造任何样本）。需人工完成一次 Reddit 登录（浏览器登录后 opencli 复用会话），否则后续扩充持续少一个一手来源 | 2026-09-11 |
| ⏳ | ↑ 同上 · **9/12 恶化：Steam 社区通道也断了** | themound 9/12 实测：**`steamcommunity.com` 全域 ECONNREFUSED**（讨论区取不到）、Reddit 全域不可达（www/old 403 + ECONNREFUSED、三个镜像 000、`site:reddit.com` 零结果）。→ 反馈采集的**两大一手来源同时缺失**，本轮改用官方评测 API（appreviews）取 100 条一手样本，并在页面 Sources 段加 Honesty note 向读者明示缺失维度。**未编造任何 Reddit/讨论区内容**。若持续，建议：①排查本机到 steamcommunity.com 的网络（是否与 GA4/GSC 同源的代理问题）；②或正式把「官方评测 API + 官方公告 RSS」登记为降级通道写入 Profile 18，避免每天重新发现 | 2026-09-12 |
| ⏳ | Dashboard GA4 Data API 凭据 | dashboard 项目的 GA_CLIENT_EMAIL/GA_PRIVATE_KEY 为空（.env.ga4 拉取结果为空串），本地脚本与 Vercel 部署均无法拉取 PV 数据；/api/diagnostics 还被 SSO 登录保护。需在 GA 后台创建 Data API service account 并授权各 Property（或找回原凭据），用 vercel env add 配置到 dashboard 项目，同时决定 /api/diagnostics 的访问方式（保持保护则监控需用 Vercel CLI/登录态访问）。完成后主 Agent 可重启性能监控链路（.agent/monitor-report.md） | 2026-08-25 |
| ⏳ | ↑ 同上 · **9/12 复核：凭据不可 pull，须重新生成** | 已确认 `vercel env ls` 中 GA_CLIENT_EMAIL / GA_PRIVATE_KEY 存在于 dashboard 项目（Production，74 天前创建），但两者被标记为 **Sensitive** → `vercel env pull` 只回写占位串 `"[SENSITIVE]"`，**无法经 CLI 找回原值**；仓库内与 git 历史中也无 service account JSON 副本。`node scripts/ga4-pv-rank.mjs` 报 `does not contain a client_email field`。可行的两条路：①在 GCP 重新签发 service account key（或新建 SA）并在 GA 后台授权各 Property → `vercel env rm` 旧值 + `vercel env add` 新值（**add 时不要勾 Sensitive**）→ 本地 `vercel env pull .env.ga4` 恢复；②若原 JSON 有备份，直接覆盖 `dashboard/.env.ga4` 的 GA_* 两行。**影响**：PV>1000 名单自 9/2 快照后无法刷新，监控员链路（⏸️）无法重启 | 2026-09-12 |
| ⏳ | 定期查看邮箱并回复用户邮件 | Contact 页邮箱 zhaoq8278@gmail.com。每周至少检查 2 次（如周一/周四）。回复内容：纠错感谢、建议评估、技术问题转主 Agent | 2026-08-24 |
| ⏳ | 用户反馈 → 内容需求评估 | 收到玩家邮件/反馈后，判断是否需要新增攻略。需新增时把反馈内容发给主 Agent，派反馈分析师做缺口分析 | 2026-08-24 |

## 已完成

| 状态 | 任务 | 详情 | 记录日期 | 完成日期 |
|:--:|------|------|:--:|:--:|
| ✅ | AdSense 申请提交 | themoundwiki + sephiriawiki 两站已提交审核 | 2026-08-24 | 2026-08-24 |
| ✅ | Welcome to Elderfield 最低配置人工核验 | QA-DEEP 2026-09-02 已通过 WebFetch 逐字核验 Steam 商店页（App 3195440）与站内表一致，system-requirements.md 的 [Unconfirmed] 已升级为 confirmed，无需人工 | 2026-08-30 | 2026-09-02 |

---

## 使用说明

- **主 Agent 职责**：发现需要人工的事项（GA4 配置、AdSense 跟进、域名注册等）MUST 写入本文件「待处理」区
- **用户职责**：定期（建议每周 2 次）检查本文件，处理后把状态改为 ✅ 并填完成日期
- **闭环**：用户处理完告知主 Agent → 主 Agent 收尾（配置/部署/记录）→ 移入「已完成」
