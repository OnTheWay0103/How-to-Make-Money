# FIX-9/11-P0 — QA Deep 遗留项 P0 修复报告

- **任务编号**: FIX-9/11-P0（源自 QA-DEEP-9/10 全量审计 `qa-deep-partB.md` §4.1 / §4.2 / §4.3 / §4.4 / §6）
- **执行**: 子 Agent（games-site）
- **日期**: 2026-09-11
- **范围**: 9 站（A 组 6 站 + B/D/E 各 1 站）
- **未提交**: 所有改动留在工作区，由主 Agent 统一 commit

---

## 零、结论速览

| 站点 | 项 | 结论 | 线上内容级验证 |
|------|----|------|:--:|
| lunariumwiki | A 页脚+Terms IP 归属 | FIXED | ✅ PASS |
| mistfallhunterwiki | A 页脚+Terms+schema | FIXED | ✅ PASS |
| shiftatmidnightwiki | A 页脚+Terms | FIXED | ✅ PASS |
| taivalwiki | A 页脚+Terms | FIXED | ✅ PASS |
| boneholdwiki | A 页脚+Terms+schema | FIXED | ✅ PASS |
| ironnestwiki | A Terms（仅此一处） | FIXED | ✅ PASS |
| anomalypresidentwiki | B `G-PLACEHOLDER` | FIXED | ✅ PASS（渲染 0 次） |
| relicfirstguardianwiki | D related 悬空 slug | FIXED | ✅ PASS（4/4 链接 200） |
| minegeonwiki | E README 他站内容 | FIXED | ✅ 无外泄（README 不渲染） |
| spiritvalewiki / grainrotwiki / themoundwiki | C 跳过 | NO-CHANGE（并发保留，未触碰，见 §5） | — |

**外部核验总原则**：QA 报告列的 7 组归属值**全部经 ≥2 独立外部来源复核，无一被推翻**。
核验过程另发现一个**站内交叉印证**：6 站中有 5 站的 `app/about/page.tsx`「not affiliated」名单**早已写对**
（与 QA 报告一致），错误的只有 Footer / Terms /（mistfall 的）schema —— 即同一站内三套值互相矛盾。

---

## 一、A 组 — 6 站页脚 + Terms 的 IP 归属错填

### 1.1 lunariumwiki

| 项 | 内容 |
|----|------|
| 问题 | Footer / Terms 写 `tinyBuild`（与本站无关的发行商） |
| 外部核验 | 开发 **Lunarium Game Studio**（上海）/ 发行 **Imperfect Games** |
| 来源 | gamespress 官方新闻稿 <https://www.gamespress.com/en-GB/Dual-Protagonist-Fantasy-ARPG-Lunarium-Breaks-Through-Darkness-on-PC-J#1>；Gematsu <https://www.gematsu.com/2026/07/lunarium-launches-july-29>；gamesurf <https://www.gamesurf.it/en/news/lunarium-launches-today-on-steam-the-launch-trailer> |
| 站内交叉印证 | `app/about/page.tsx:17` 早已写「developed by Lunarium Game Studio and published by Imperfect Games」 |
| 改动 | `components/Footer.tsx:24`、`app/terms/page.tsx:26` → `Lunarium Game Studio / Imperfect Games` |
| schema | `lib/schema.ts` 原本**已正确**（Lunarium Game Studio / Imperfect Games），无需改 → 三处现一致 |

### 1.2 mistfallhunterwiki

| 项 | 内容 |
|----|------|
| 问题 | Footer / Terms 写 `Proxima Studio`；**schema 写 `Unknown`** |
| 外部核验 | 开发 **Bellring Games** / 发行 **Skystone Games** |
| 来源 | SteamDB <https://steamdb.info/bundle/76900/>；4p.de <https://www.4p.de/news/rollenspiel-steam-bellring-games-publisher-skystone-games-mistfall-hunter>；gamebiz <https://gamebiz.jp/news/429969>；Polygon <https://www.polygon.com/mistfall-hunter-1-million-players-negative-reviews/> |
| 站内交叉印证 | `app/about/page.tsx:53` 早已写「Bellring Games, Skystone Games」 |
| 改动 | `components/Footer.tsx:24`、`app/terms/page.tsx:26` → `Bellring Games / Skystone Games`；`lib/schema.ts:67,71` `Unknown` → `Bellring Games` / `Skystone Games` |
| 备注 | 这是 6 站中唯一 schema 也需改的站（QA 报告称「多数站 JSON-LD 是对的」，本站是例外） |

### 1.3 shiftatmidnightwiki

| 项 | 内容 |
|----|------|
| 问题 | Footer / Terms 写 `Tasty Stewdios` |
| 外部核验 | 开发 **Bun Muen**（澳 solo）/ 发行 **Kwalee** |
| 来源 | Xbox 商店 <https://www.xbox.com/en-IN/games/store/shift-at-midnight/9n0wdpmxnhwn>（「Developed by Bun Muen, exclusively licensed to Kwalee Ltd.」）；4gamer <https://www.4gamer.net/games/024/G102459/20260723043/>；Gamereactor <https://www.gamereactor.eu/shift-at-midnight-196463/> |
| 站内交叉印证 | `app/about/page.tsx:15` 早已写「Bun Muen ... Kwalee」 |
| 改动 | `components/Footer.tsx:24`、`app/terms/page.tsx:26` → `Bun Muen / Kwalee` |
| schema | 原本已正确（Person `Bun Muen` / Organization `Kwalee`），无需改 |

### 1.4 taivalwiki

| 项 | 内容 |
|----|------|
| 问题 | Footer / Terms 写 `Mingle Games` |
| 外部核验 | 开发 **Bonobo Software**（芬兰）/ 发行 **Level Up Gaming** |
| 来源 | gamespress <https://www.gamespress.com/it/START-THE-JOURNEY-TODAY---TAIVAL-OUT-NOW-ON-STEAM-EARLY-ACCESS#1>；Game*Spark <https://www.gamespark.jp/article/2026/07/15/169300.html>；IT之家 <https://m.ithome.com/html/976764.htm> |
| 站内交叉印证 | `app/about/page.tsx:15` 早已写「Bonobo Software ... Level Up Gaming」 |
| 改动 | `components/Footer.tsx:24`、`app/terms/page.tsx:26` → `Bonobo Software / Level Up Gaming` |
| schema | 原本已正确，无需改 |

### 1.5 boneholdwiki

| 项 | 内容 |
|----|------|
| 问题 | Footer / Terms 写 `Soulash Studios`；schema author 只写 `Pixel Jackal`（漏 SaikingS） |
| 外部核验 | 开发 **Pixel Jackal + SaikingS** / 发行 **Pixel Jackal** |
| 来源 | datahumble <https://datahumble.com/bonehold-3497800>（直读：「developer is listed as "Pixel Jackal, SaikingS" and the publisher as "Pixel Jackal"」）；Razer <https://deals.razer.com/games/bonehold>；SteamDB <https://steamdb.info/app/3497800/> |
| 改动 | `components/Footer.tsx:24`、`app/terms/page.tsx:26` → `Pixel Jackal / SaikingS`；`lib/schema.ts:62` author → `Pixel Jackal & SaikingS`（publisher `Pixel Jackal` 本就正确，保留） |
| 与 QA 报告的分歧 | QA 报告写「Pixel Jackal（+SaikingS，开发/发行）」易读成 SaikingS 也是发行；外部来源一致显示**发行只有 Pixel Jackal**。以外部来源为准，本报告按「开发=Pixel Jackal + SaikingS / 发行=Pixel Jackal」写入 |
| 遗留 | `app/about/page.tsx:51` not-affiliated 名单只列 `Pixel Jackal`（未含 SaikingS）—— 表述为真、不算错误，且**不属本任务范围**，未改 |

### 1.6 ironnestwiki（仅 Terms 错）

| 项 | 内容 |
|----|------|
| 问题 | `app/terms/page.tsx:26` 写 `House House / Panic` —— 这是**他站 bigwalkwiki** 的工作室（跨站污染） |
| 外部核验 | 开发+发行 **Nick Nieuwoudt & Dominik Latos**（自发行） |
| 来源 | Wikipedia <https://en.wikipedia.org/wiki/Iron_Nest>；SteamDB <https://steamdb.info/app/2950790/>；gamepressure <https://www.gamepressure.com/companies/nick-nieuwoudt-dominik-latos/zb3620>；GOGDB <https://www.gogdb.org/product/1162687982>（GOGDB 单独把 Dominik Latos 标为 publisher，属细节差异，不影响本站「两人自发行」表述） |
| 站内交叉印证 | `components/Footer.tsx:24` 早已写 `Nick Nieuwoudt and Dominik Latos`；`app/about/page.tsx:52` 同 |
| 改动 | 仅 `app/terms/page.tsx:26` → `Nick Nieuwoudt &amp; Dominik Latos`（JSX 实体，渲染为 `&`，与 schema 的 `&` 一致） |
| Footer / schema | 均原本正确，未改 |

### 1.7 A 组 — 与 QA 报告的分歧汇总

- **无任何一站被外部来源推翻**。6 站 7 组归属值与 QA 报告完全一致。
- 唯一细节修正：**boneholdwiki 的发行商**（QA 表述含混，已按外部来源明确为仅 Pixel Jackal）。
- 新增发现（QA 报告未提）：**mistfallhunterwiki 的 `lib/schema.ts` 是 `Unknown/Unknown`**，并非 QA 所说「JSON-LD 多数是对的」中的一员；已一并修正。

---

## 二、B — anomalypresidentwiki 渲染 `G-PLACEHOLDER`

| 项 | 内容 |
|----|------|
| 问题 | `lib/seo-config.ts:16` `googleAnalyticsId: 'G-PLACEHOLDER'`；`components/GoogleAnalytics.tsx` 只判空 → 线上每页渲染 gtag 并向 Google 发无效测量 ID 请求 |
| 事前线上取证 | `curl https://anomalypresidentwiki.vercel.app/` 命中 `G-PLACEHOLDER` ✅（本次修复前实测） |
| 改动 | `lib/seo-config.ts:16` → `''`；`dashboard/lib/sites.ts:208`（Anomaly President Wiki 记录）`gaId: 'G-PLACEHOLDER'` → `''` |
| 一致性 | 另 5 站（bombanana / crimsonmoon / doloctown / fallentear / sovereigntower / welcomeelderfield）为 `''` 的既有做法；dashboard 注册表现与该站一致 |
| 未改 | `dashboard/lib/sites.ts` 另外 4 条 `G-PLACEHOLDER`（crimsonmoon / welcomeelderfield / fallentear / bombanana）—— 那些站的**站内**值本就是 `''`，属 QA §6 记录的🟢语义等价项，**不属本任务范围**，未动 |

---

## 三、D — relicfirstguardianwiki related 悬空 slug

| 项 | 内容 |
|----|------|
| 问题 | `content/guides/secret-areas.md:8` `related` 含 `boss-guide`，本站实际文件为 `boss-strategies.md` → `app/guides/[slug]/page.tsx` 会渲染出 404 链接 |
| 事前核对 | 已 `ls content/guides/` 逐一比对**全站 30 篇指南的 related 数组（共 30 行）**：除 `boss-guide` 外**全部 slug 均真实存在** |
| 改动 | `content/guides/secret-areas.md:8` → `related: ["walkthrough", "boss-strategies", "runes-guide", "all-bosses-list"]` |
| 备注 | 仅此一处悬空；无其他 related 需修 |

---

## 四、E — minegeonwiki README 他站内容

| 项 | 内容 |
|----|------|
| 问题 | `README.md:1,3,7` 整篇为 **Skills & Raids Wiki** 的 README，含他站 URL `https://skillsandraidswiki.vercel.app` 及他站开发商 Lootsurvivor（不渲染，但属跨站残留） |
| 改动 | 重写为 minegeonwiki 自己的 README，沿用原模板结构（Site / Content 表 / Tech Stack / Development / Deployment） |
| 内容来源 | 全部取自本仓库自身、可验证：`lib/seo-config.ts`（站名 MineGeon Wiki、URL、描述）、`content/guides/*.md` 的 27 篇指南与各自 frontmatter `category`、`package.json`（Next 16 / React 19 / TS / Tailwind 4）、`vercel.json`（`installCommand: pnpm install`） |
| 宁缺勿编 | **未写开发/发行商名**、未写任何未经核实的游戏事实、未写发售日/价格 |
| 顺带修正 | 原模板的 `npm install` / `npm run dev` 改为 `pnpm`（符合 CLAUDE.md「wiki 站 MUST 用 pnpm」） |
| 残留复查 | `grep -rn "skillsandraid|Skills & Raids|Lootsurvivor" minegeonwiki` → **0 命中** |

---

## 五、C — 并发保留（未触碰）

按任务指令，以下 3 站**任何文件均未修改**（另有子 Agent 并发处理）：
`spiritvalewiki`（/tier-list 404）、`grainrotwiki`（Footer/Terms）、`themoundwiki`（EX-Mod meta + ending-guide 404）。

---

## 六、线上内容级验证

> 验证口径：`curl` 抓取线上页脚所在页面（`/`）与 `/terms` 正文，**确认新值已渲染**（不看首页 HTTP 200，不看 build 退出码）。

### 6.1 修复前基线（旧部署，2026-09-11 05:0x 实测）

| 站点 | 旧 token | 在 `/` 命中 | 在 `/terms` 命中 |
|------|---------|:--:|:--:|
| lunariumwiki | `tinyBuild` | 1 | 1 |
| mistfallhunterwiki | `Proxima Studio` | 1 | 1 |
| shiftatmidnightwiki | `Tasty Stewdios` | 1 | 1 |
| taivalwiki | `Mingle Games` | 1 | 1 |
| boneholdwiki | `Soulash Studios` | 1 | 1 |
| ironnestwiki | `House House` | **0**（页脚本就正确） | 1 |
| anomalypresidentwiki | `G-PLACEHOLDER` | 1 | 1 |

基线**与 QA 报告完全吻合**，且印证 ironnestwiki「只有 terms 错」。

### 6.2 部署与修复后验证

**部署**: 9 站全部 `./deploy-wiki-site.sh <site>`（Vercel CLI 远端构建），每站均拿到新 deployment URL：

```
lunariumwiki            https://lunariumwiki-26l3vpbc2-zhaoq0103s-projects.vercel.app
mistfallhunterwiki      https://mistfallhunterwiki-885ycmxg1-zhaoq0103s-projects.vercel.app
shiftatmidnightwiki     https://shiftatmidnightwiki-8blrjefx2-zhaoq0103s-projects.vercel.app
taivalwiki              https://taivalwiki-ekd2rdnx6-zhaoq0103s-projects.vercel.app
boneholdwiki            https://boneholdwiki-dx9lxje1a-zhaoq0103s-projects.vercel.app
ironnestwiki            https://ironnestwiki-74asourv7-zhaoq0103s-projects.vercel.app
anomalypresidentwiki    https://anomalypresidentwiki-9x3079kge-zhaoq0103s-projects.vercel.app
relicfirstguardianwiki  https://relicfirstguardianwiki-ll43gfq0p-zhaoq0103s-projects.vercel.app
minegeonwiki            https://minegeonwiki-3m8k32nwq-zhaoq0103s-projects.vercel.app
```

> **脚本退出码不可信（并发误报，符合 04 Profile 记录）**：9 站里除 lunariumwiki 第二次重试外，
> 脚本**全部报 `exit 1`**，但其中 8 次实际部署成功。根因：脚本第 3 步
> `ALIAS_URL=$(vercel inspect ... | grep -o ... | head -1)` 在 `set -euo pipefail` 下，
> 若 `vercel inspect` 输出无 URL 则 grep 返回 1 → 整个赋值语句触发 errexit，
> 脚本在打印 `✅ 部署完成` **之前**静默退出。（lunariumwiki 首次是真失败：`"message": "Not authorized"`，
> 与并发 agent 的 token 刷新竞争有关，第二次重试成功。）
> **故本轮一律以内容级 curl 验证为准**，不以退出码为准。

**修复后线上实测（`/` 页脚 + `/terms` 正文，curl 抓取渲染后 HTML）**：

| 站点 | 线上页脚渲染值 | 线上 Terms 渲染值 | 旧值残留 | 判定 |
|------|---------------|------------------|:--:|:--:|
| lunariumwiki | `Lunarium Game Studio / Imperfect Games` | `Lunarium Game Studio / Imperfect Games` | 0 | ✅ PASS |
| mistfallhunterwiki | `Bellring Games / Skystone Games` | `Bellring Games / Skystone Games` | 0 | ✅ PASS |
| shiftatmidnightwiki | `Bun Muen / Kwalee` | `Bun Muen / Kwalee` | 0 | ✅ PASS |
| taivalwiki | `Bonobo Software / Level Up Gaming` | `Bonobo Software / Level Up Gaming` | 0 | ✅ PASS |
| boneholdwiki | `Pixel Jackal / SaikingS` | `Pixel Jackal / SaikingS` | 0 | ✅ PASS |
| ironnestwiki | `Nick Nieuwoudt and Dominik Latos`（原本就对） | `Nick Nieuwoudt & Dominik Latos` | 0 | ✅ PASS |
| anomalypresidentwiki | `G-PLACEHOLDER` = **0 次**，`googletagmanager` = **0 次** | — | 0 | ✅ PASS |

**旧值全站清零复扫**（`/`、`/terms`、`/about`、`/privacy` 四页合计，curl 实测）：
`tinyBuild`=0、`Proxima`=0、`Tasty`=0、`Mingle`=0、`Soulash`=0、`House House`=0、`G-PLACEHOLDER`=0。

**实体转义核验**（ironnestwiki）：线上 HTML 为 `respective owners (Nick Nieuwoudt &amp; Dominik Latos)`，
浏览器解码后为 `Nick Nieuwoudt & Dominik Latos`（Python `html.unescape` 复核），与 schema 的 `&` 一致，无 `&amp;amp;` 双重转义。

**D 项（relicfirstguardianwiki）线上验证**：
`curl https://relicfirstguardianwiki.vercel.app/guides/secret-areas` → HTTP 200，
页面 `/guides/` 链接集合 = `{all-bosses-list, boss-strategies, runes-guide, secret-areas, walkthrough}`；
`/guides/boss-guide` 出现 **0 次**；4 个 related 目标逐条 curl 全部 **HTTP 200**。

**E 项（minegeonwiki）验证**：README 不参与渲染，故验证口径为「不再外泄他站信息 + 站点正常」：
- `https://minegeonwiki.vercel.app/README.md` → **HTTP 404**（README 不在 `public/`，本就不对外）
- 首页 `skillsandraidswiki` 出现 **0 次**
- 首页 HTTP 200（站点正常）
- 仓库内 `grep -rn "skillsandraid|Skills & Raids|Lootsurvivor" minegeonwiki` → **0 命中**

---

## 七、改动文件清单（工作区，未 commit）

```
anomalypresidentwiki/lib/seo-config.ts
boneholdwiki/app/terms/page.tsx
boneholdwiki/components/Footer.tsx
boneholdwiki/lib/schema.ts
dashboard/lib/sites.ts
ironnestwiki/app/terms/page.tsx
lunariumwiki/app/terms/page.tsx
lunariumwiki/components/Footer.tsx
minegeonwiki/README.md
mistfallhunterwiki/app/terms/page.tsx
mistfallhunterwiki/components/Footer.tsx
mistfallhunterwiki/lib/schema.ts
relicfirstguardianwiki/content/guides/secret-areas.md
shiftatmidnightwiki/app/terms/page.tsx
shiftatmidnightwiki/components/Footer.tsx
taivalwiki/app/terms/page.tsx
taivalwiki/components/Footer.tsx
```

不属本任务的既有工作区改动（**未触碰**）：`.agent/qa-deep-partB.md`（任务开始前即为 `M` 状态）。

---

## 八、未改项及原因

| 项 | 原因 |
|----|------|
| spiritvalewiki / grainrotwiki / themoundwiki | 任务指令 C：并发保留，MUST NOT 触碰 |
| `app/about/page.tsx` 的 not-affiliated 名单 | 表述为真（未含完 SaikingS 属不完整而非错误）；不属本任务三处（Footer/Terms/schema）范围 |
| `dashboard/lib/sites.ts` 其余 4 条 `G-PLACEHOLDER` | 对应站内值为 `''`，语义等价（QA §6 定为🟢），不属本轮 P0 |
| 25 站 schema JSDoc `/guides/crafting-guide`、9 站样板 SVG、home-content 孤儿、waterpark「CayPlay / CayPlay」 | QA 定为🟢顺手项，任务明确本轮不做 |
| `dashboard/lib/sites.ts` 补 doloctown / sovereigntower（§6） | 与本任务 9 站无关，任务未列 |
| `dashboard` 的部署 | 本任务只要求改 `dashboard/lib/sites.ts` 的值，**未要求部署 dashboard**（它是独立 Vercel 项目，`deploy-wiki-site.sh` 亦不含它）。该改动待主 Agent 决定是否随 dashboard 一并上线 |

---

## 九、待人工

1. **lunariumwiki 首次部署失败**（`"Not authorized"`）—— 疑似与并发 agent 争抢 Vercel CLI token 刷新有关，
   第二次重试即成功。**建议 04 Profile 记录**：并发部署时 `vercel deploy` 可能出现
   瞬时 `Not authorized`，应**自动重试 1 次**，而非直接判定该站失败。
2. **`deploy-wiki-site.sh` 的退出码缺陷（本轮再次复现，9/9 误报）** —— 第 3 步
   `ALIAS_URL=$(vercel inspect ... | grep -o ... | head -1)` 在 `set -euo pipefail` 下，
   grep 无匹配 → 赋值语句返回 1 → errexit 静默退出，**`✅ 部署完成` 之前的输出被吞**。
   本轮 8 个实际成功的站全部被误报为 `FAIL(exit 1)`。
   建议改法：`ALIAS_URL=$(... || true)` 或用 `if ! ALIAS_URL=$(...); then ALIAS_URL=""; fi`。
   **已由 QA/建站 Profile 记录过同类问题，本轮证明仍未修复。**
3. **ironnestwiki `app/about/page.tsx:51`** not-affiliated 名单为 `Nick Nieuwoudt, Dominik Latos`，
   与本轮 Terms 的 `& ` 写法并存 —— 仅为连接词差异，无实质不一致，未改。
4. **boneholdwiki `app/about/page.tsx:51`** 只列 `Pixel Jackal`，未含 SaikingS（表述为真但不完整）。
   如需完全对齐 Steam 的「Pixel Jackal, SaikingS」开发署名，可另行派单。
5. **mistfallhunterwiki `lib/schema.ts` 曾是 `Unknown/Unknown`** —— QA §4.1 称「多数站 JSON-LD 是对的」，
   本站其实是例外。建议 QA 复核其结论中「JSON-LD 正确」的站名单，确认无同类漏网。
